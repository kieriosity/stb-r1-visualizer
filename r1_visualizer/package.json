{
  "name": "r1-visualizer",
  "version": "0.1.0",
  "description": "Form-faithful visualizer for STB Form R-1 canonical JSON. Runs standalone and embeds into WordPress.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test src/*.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "vite": "^5.3.0"
  }
}
